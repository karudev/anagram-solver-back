{% extends 'base.html.twig' %}

{% block title %}Hello AnagramSolverController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

<div class="example-wrapper">
    <h1>AnagramSolver ✅</h1>

    <div>
        {{ form_start(form) }}
        <table>
            <tr>
                <td>
                    {{ form_label(form.initialString) }}
                </td>
                <td>
                    {{ form_widget(form.initialString) }}
                </td>
            </tr>
            <tr>
                <td>
                    {{ form_label(form.stringToCompare) }}
                </td>
                <td>
                    {{ form_widget(form.stringToCompare) }}
                </td>
            </tr>
            <tr>
                <td>

                </td>
                <td>
                    <button type="submit">Calcul</button>
                </td>
            </tr>
        </table>
        {{ form_end(form) }}
    </div>
    {% if p is not null %}
        <p>The number of rearrangement for <b>{{ initialString }}</b> to <b>{{ stringToCompare }}</b> is <b>{{ p }}</b></p>
    {% endif %}
</div>
{% endblock %}
